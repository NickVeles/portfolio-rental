{
  "name": "portfolio-rental",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:client": "npm run dev -w @portfolio-rental/client",
    "dev:server": "npm run dev -w @portfolio-rental/server",
    "build": "npm run build --workspaces --if-present",
    "prisma:generate": "npm run prisma:generate -w @portfolio-rental/server",
    "seed": "npm run seed -w @portfolio-rental/server"
  }
}
